{
  "name": "cicd-beginner",
  "private": true,
  "version": "0.1.0",
  "description": "Beginner CI/CD sample: React client + Node server with GitHub Actions.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
